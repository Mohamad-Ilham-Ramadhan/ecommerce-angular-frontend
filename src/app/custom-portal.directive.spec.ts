import { CustomPortalDirective } from './custom-portal.directive';

describe('CustomPortalDirective', () => {
  it('should create an instance', () => {
    // const directive = new CustomPortalDirective();
    // expect(directive).toBeTruthy();
  });
});
